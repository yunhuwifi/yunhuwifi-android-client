<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white" 
    android:orientation="vertical"
     tools:context=".ExpandActivity">

    <LinearLayout
        android:id="@+id/ll_index_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/normalframetitlebg"
        android:baselineAligned="false"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/tv_storage"
            style="@style/menuTitle"
            android:background="@drawable/pressedframetitlebg"
            android:text="存储盘"
            android:textColor="@color/frametitlecolor" />

        <TextView
            android:id="@+id/tv_skydrive"
            style="@style/menuTitle"
            android:background="@drawable/normalframetitlebg"
            android:text="网盘"
            android:textColor="@color/black" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_file"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        />

</LinearLayout>